# solland_old_legacy = {
	# type = sollander_heroism
	# quality = famed
	# is_shown = {
		# has_dlc_feature = legends
		# NOT = {
			# has_global_variable = carolingian_legend
		# }
		# OR = {
			# culture = { has_cultural_pillar = heritage_bretonnian }
			# culture = { has_cultural_pillar = heritage_sartosan }
			# culture = { has_cultural_pillar = heritage_tilean }
		# }
	# }
	# is_valid = {
		# has_title = title:k_solland
		# prestige_level >= medium_prestige_level
	# }

	# chronicle = solland_valiant_defense
	# chronicle_properties = {
		# ancestor = character:1700
		# title = title:k_solland
	# }
# }

# drakwald_old_legacy = {
	# type = drakwald_heroism
	# quality = famed
	# is_shown = {
		# has_dlc_feature = legends
		# NOT = {
			# has_global_variable = carolingian_legend
		# }
		# OR = {
			# culture = { has_cultural_pillar = heritage_bretonnian }
			# culture = { has_cultural_pillar = heritage_sartosan }
			# culture = { has_cultural_pillar = heritage_tilean }
		# }
	# }
	# is_valid = {
		# has_title = title:k_drakwald
		# prestige_level >= medium_prestige_level
	# }

	# chronicle = struggle_conclusion_drakwald
	# chronicle_properties = {
		# ancestor = character:1700
		# title = title:k_drakwald
	# }
# }